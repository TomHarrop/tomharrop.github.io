% Layout
\setuppapersize[A4]
\setuplayout[width=fit, height=middle,
             leftmargin=20mm, rightmargin=20mm,
             topspace=30mm, bottomspace=30mm,
             header=0mm, footer=0mm]
\setupalign[flushleft, nothyphenated, hanging]
\setupwhitespace[medium]

% PDF title
$if(title)$
\setupinteraction[state=start, title=$title$]
$endif$

% Type
\usetypescript[lato]
\setupbodyfont[lato, 10pt]
\definefont[ms][Mono sa 0.95]
% \defineframed
%   [dohotkey]
%   [corner=00, location=low, backgroundoffset=-0.3em,
%    background=color, backgroundcolor=gray, frame=off]
% \definehspace[hotkey][0.5em]
% \def\hotkey#1{\dohotkey{\hspace[hotkey]#1\hspace[hotkey]}}
% \setuptype[style=\ms, command=\hotkey]
\setuptype[style=\ms]
\setuplanguage[en][spacing=packed]
\language[en]
\setbreakpoints[compound]

% Colour
\setupcolor[hex]
\definecolor[sectioncolor]
  [h=2c3e50]

% Hyperlinks
\setupinteraction[state=start, color=sectioncolor, style=\bf\underbar]

% Headings
\setuphead[chapter]
  [style=\tfd]
\setuphead[section]
  [style=\tfb\bf, color=sectioncolor, align=middle]
\setuphead[subsection]
  [style=\tfa\bf, color=sectioncolor, align=flushleft]
\setuphead[subsubsection]
  [style=\bf, color=sectioncolor, align=flushleft]

\setuphead[chapter, section, subsection, subsubsection]
  [number=no, before={\blank[big]}, after={\blank[small]}]

% Descriptions
\definedescription[description]
  [alternative=left, width=6em,
   headstyle=bold, style=normal,
   headalign=flushright,
   before={\setupnarrower[right=4em]%
           \startnarrower[right]},
   after={\stopnarrower}]

% negative indent for reference section
\definestartstop[negindent]
  [before={\startnarrower[left]%
           \setupindenting[-\leftskip,yes,first]%
           \setuphead[subsection][indentnext=yes]},
   after=\stopnarrower]

% width of horizontal rules
\define\thinrule{%
  \startalignment[center]%
  \midaligned{%
  \blackrule[width=150mm, height=.5\linewidth, depth=.5\linewidth]}%
  \stopalignment
  }

% centre blockquotes
\setupdelimitedtext[blockquote]
  [before={\setupalign[middle]},
   indentnext=no]

\starttext

$if(title)$
\section[title]{$title$}
$endif$

$body$

\stoptext
